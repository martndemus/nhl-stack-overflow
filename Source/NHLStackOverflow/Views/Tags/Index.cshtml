@{
    ViewBag.Title = "Index";
    
    if (Request.Headers["X-PJAX"] == null)
    {
        Layout = "~/Views/Layouts/_Main.cshtml";
    }
    else
    {
        Layout = null;
    }
}

<h2>Index</h2>
<ul>
@foreach (var tag in ViewBag.TagList)
{
    <li><h3>@Html.ActionLink((string)tag.Name, "Search", "Tags", new {id = tag.TagID}, new {target = "blank"}) x @tag.Count</h3>
    @tag.Description
    
    </li>
}
</ul>